apiVersion: v1
kind: Secret
metadata:
  name: inventory-secret
  namespace: inventory
type: Opaque
# In production, do not commit plaintext credentials.
# Use Sealed Secrets (https://github.com/bitnami-labs/sealed-secrets)
# or External Secrets Operator (https://external-secrets.io) instead.
stringData:
  DB_USERNAME: postgres
  DB_PASSWORD: postgres
