server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/inventory_dev
    username: ${DB_USERNAME:nikhil}
    password: ${DB_PASSWORD:}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true

logging:
  level:
    com.example.inventory: DEBUG
